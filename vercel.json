{
  "buildCommand": "cd frontend && npm install && npx expo export -p web",
  "outputDirectory": "frontend/dist",
  "cleanUrls": true,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
